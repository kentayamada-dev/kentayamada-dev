name: Scheduled-Workflow

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '10 * * * *'

jobs:
  Update-README:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install chrome
        run: make install-chrome

      - name: Install necessary packages
        run: make install-packages

      - name: Update README
        run: make generate

      - name: Update assets
        run: mv assets_temp assets

      - name: Commit assets
        uses: EndBug/add-and-commit@v9.1.0
        with:
          add: assets
          pull: origin main

      - name: Commit README.md
        uses: EndBug/add-and-commit@v9.1.0
        with:
          add: README.md
          pull: origin main
